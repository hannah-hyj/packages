name: Batch Release
on:
  push:
    branches:
      - 'release-go_router'
permissions:
  contents: write # Release needs to push a tag back to the repo.
jobs:
  release:
    uses: ./.github/workflows/reusable_release.yml
    with:
      is-batch-release: true
      branch-name: '${{ github.ref_name }}'
    secrets: inherit
